# Calendar

## Please add .babelrc file with following content to the root directory, otherwise the app won't run

```
  {
    "presets": [
      "es2015",
      "react",
      "stage-0"
    ]
  }
```
